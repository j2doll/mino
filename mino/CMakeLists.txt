#------------------------------------------
# NOTE: 라이브러리 간에 종속성이 있는 경우,
# 상위 라이브러리를 더 위에 구성.
#------------------------------------------
add_subdirectory(core)
add_subdirectory(external)
add_subdirectory(network)
