{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++: Build and Debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/program_name",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "preLaunchTask": "CMake Build Preset",

      "windows": {
        "type": "cppvsdbg",
        // TODO: VSCode 윈도(Visual Studio 컴파일러) 환경에서 디버깅할 때 사용할 프로그램 경로를 지정.
        "program": "${workspaceFolder}/out/build/${input:selectedPreset}/example/core/bit/Debug/core_bit_example.exe",
        "console": "integratedTerminal",
        // "nativeDebugging": false,
        // "launchBrowser": false
      },

      "linux": {
        "type": "cppdbg",
        "MIMode": "gdb",
        "miDebuggerPath": "/usr/bin/gdb", // TODO: gdb 경로 지정
        // TODO: VSCode 리눅스(gcc 컴파일러) 환경에서 디버깅할 때 사용할 프로그램 경로를 지정.
        "program": "${workspaceFolder}/out/build/${input:selectedPreset}/example/core/bit/core_bit_example",
        "externalConsole": false,
        "setupCommands": [
          {
            "description": "gdb pretty-printing 활성화",
            "text": "-enable-pretty-printing",
            "ignoreFailures": true
          }
        ]
      }
    }
  ],
  "inputs": [
    {
      "id": "selectedPreset",
      "type": "pickString",
      "description": "사용할 CMake Preset을 선택하세요",
      "options": [
        "linux-gcc-debug",
        "linux-gcc-release",
        "vs-debug",
        "vs-release"
      ],
      "default": "linux-gcc-debug"
    }
  ]
}
